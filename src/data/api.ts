export enum API_ENDPOINTS {
  signup = 'auth/signup',
  signin = 'auth/signin',
  users = 'users',
  boards = 'boards',
  boardsSet = 'boardsSet',
  columns = 'columns',
  columnsSet = 'columnsSet',
  tasks = 'tasks',
  tasksSet = 'tasksSet',
  files = 'file',
  points = 'points',
}
